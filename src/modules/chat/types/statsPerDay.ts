export interface statsPerDay {
    "statsPerDay": {
        date: {
          "messagesSent": 0
        }
      }
}
// const valueRedis = `{ "statsPerDay" : { "${inputDateStats}": { messageSent : ${count} } } }`;
// { “statsPerDay”: { “15.03”: { messagesSent: 15 } } }
